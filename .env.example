# -----------------------------------------------------------
# whoop-go Example Configuration
# -----------------------------------------------------------
# Copy this file to .env and fill in your credentials:
#   cp .env.example .env && source .env
#
# Then run the example:
#   make build-local && ./bin/example
# -----------------------------------------------------------

# Your WHOOP Developer OAuth2 access token.
# Generate this by running: go run cmd/auth/main.go
# See README.md "Getting an OAuth Token" for details.
export WHOOP_OAUTH_TOKEN=""

# Your WHOOP Webhook signing secret.
# Configured when registering a webhook endpoint in the Developer Portal.
export WHOOP_WEBHOOK_SECRET=""
